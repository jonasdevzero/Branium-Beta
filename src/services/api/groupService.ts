import { ContactMessage } from "../../types/user"

const groupService = {
    async getMessages() {
        return [] as ContactMessage[]
    },

    async viewMessages() {
    }
}

export default groupService
